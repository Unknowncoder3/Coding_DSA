#startswith examples
n=str(input("Enter a string: "))
start_substr=str(input("Enter the starting substring to check: "))
starts_with=n.startswith(start_substr)
print(f"Does the string start with '{start_substr}'? :", starts_with)

